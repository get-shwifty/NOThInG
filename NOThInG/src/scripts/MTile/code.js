const MTile = (self, baseSprite) => {
    const isSameType = [[0,0,0], [0,0], [0,0,0]]
    const arround = utils.getUnderDistance(self, 1)
    const own = self.type
    arround.forEach(obj => {
        // do stuff
    })
}